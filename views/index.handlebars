<h1>Burgers!</h1>

<h2 id="left">Tasty burgers ready to be ate!</h2>

<ul class="newstyle" id="ready">
  {{#each burgers}}
  {{#if devoured}}
  {{> burgers/burger-block devoured=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>Those who have been devoured. RIP fam.</h2>

<ul class="newstyle" id="eaten">
  {{#each burgers}}
  {{#unless devoured}} 
  {{> burgers/burger-block devoured=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2 id="right">Add a Burger</h2>
<form id="create-form">

  <div class="form-group">
    <label class="burger" for="ca">Burger Name:</label>
    <input type="text" id="burg" name="name">
  </div>

  <button class="submit" type="submit">Add dat burg</button>
</form>